% layout 'default';
% if (my $msg = flash 'message') {
  <b><%= $msg %></b><br>
% }
Welcome <%= session 'user' %>.<br>
%= link_to Logout => 'logout'
%= link_to Logout => '/info'
%= link_to Logout => '/pi-status'
%= link_to Logout => '/bootstrap'

